import 'package:flutter/foundation.dart';

abstract class Constants {
  static double defaultPadding = kIsWeb ? 40 : 20;
  static const appName = "Shop Controller";
  static const appFontName = "Lexend";
}
